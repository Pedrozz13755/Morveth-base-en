const { prefix, botName, ownerName, ownerJid, ownerLid, ownerLink } = require("../../owner/config.json")

module.exports = {
  nome: "NOME",
  descricao: "DESCRIÇÃO",
  comandos: ["t", "COMANDO2"],
  uso: `${prefix}t`,

  handle: async ({ morveth, info, from, fotomenu, selo }) => {
//CÓDIGO 
  },
};